<?php

/**
 * SeminarReferenten
 *
 * This class has been auto-generated by the Doctrine ORM Framework
 *
 * @package    ##PACKAGE##
 * @subpackage ##SUBPACKAGE##
 * @author     ##NAME## <##EMAIL##>
 * @version    SVN: $Id: Builder.php 5441 2009-01-30 22:58:43Z jwage $
 */
class SeminarReferent extends BaseSeminarReferent
{
	public function setUp() {
		parent::setUp();

		$this->hasOne('Referent', array (
			'local'=>'referent_id',
			'foreign'=>'id',
			)
		);

		public function setUp() {
			parent::setUp();
			$this->actAs("ChangeCounted");
		}
	}

	function getReferentenForSeminarIdTableProxy ($id, $standort_id) {
		$q  = Doctrine_Query::create();
		$q->from ( "SeminarReferent semRef");
		$q->leftJoin("semRef.Referent referent");
		$q->where ("semRef.seminar_id = ?", array($id));
		$q->andWhere ("semRef.standort_id = ?", array($standort_id));
		$q->orderBy("semRef.tag ASC");

		return $q->fetchArray();
	}



	function getBySeminarIdTableProxy ($seminar_id, $referent_id=-1) {
		$q = Doctrine_Query::create();
		$q->from ("SeminarReferent semRef");
		$q->where ("semRef.seminar_id = ?", $seminar_id);

		if ( $referent_id != -1 ) {
			$q->andWhere ("semRef.referent_id = ?", $referent_id);
		}

		if ( $q->count() > 0 ) {
			return $q->execute();
		} else {
			return NULL;
		}
	}

	/**
	* Loescht alle referenten des ensprechenden seminars
	* @param Int $seminar_id
	* @return String
	*/
	function clear($seminar_id) {
		$q = Doctrine_Query::create();
		$q->delete ("SeminarReferent semRef");
		$q->where ("semRef.seminar_id = ?", $seminar_id);
		$q->andWhere ( "semRef.tag = ?", $tag);

		return $q->execute();
	}

}